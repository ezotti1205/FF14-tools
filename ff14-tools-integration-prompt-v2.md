# FF14 ギャザクラ支援ツール — 統合版（タブ切り替え1ページ・3本構成）Claude Code 用 指示文

対象は以下の3本です（釣りツールは実装見送りのため対象外）。
1. 時間限定ノードのカウントダウン
2. 総資産記録帳（旧・収支ログから方向転換したもの）
3. 「作る vs 買う」判定ツール

すでに個別に実装・テスト済みのコードがある前提の指示文です。

---

## 統合の指示文

```
FF14のギャザラー・クラフター向け支援ツール3本を、タブ切り替えで1つの画面にまとめた
Webアプリに統合してください。ブラウザのタブは1つで完結させたいです。

【対象の3機能】
1. 時間限定ノードのカウントダウン
2. 総資産記録帳（総資産額の推移をグラフで見る帳簿ツール）
3. 「作る vs 買う」判定ツール

それぞれ個別に実装・動作確認済みのコードがこのフォルダにあります。
まずは現在のファイル構成を確認して、3つがそれぞれどのファイル/フォルダに
対応するか把握した内容を教えてください。

ロジック部分（ET計算、Universalis連携、資産グラフの集計処理など）はそのまま流用し、
UIの外枠だけをタブ構成に組み替える方針で進めてください。
何かを大きく作り直す必要が出てきた場合は、着手前に理由を説明してください。

【全体構成】
- 画面上部に固定のタブバー（ノード / 総資産 / 作る vs 買う の3タブ）
- タブバーの右端に、エオルゼア時間（ET）と現実時刻の共通表示を常駐させる
  （ノードツールが使っているET計算ロジックを共通モジュールとして切り出し、
  他タブからも参照できるようにする）
- 各タブの中身は独立したコンポーネント／モジュールとして分離し、
  1ファイルに全部べた書きしないこと（保守性を優先）
- タブを切り替えても、裏のタブのカウントダウンや通知判定は止めない
  （タブが非表示の間はブラウザ側でタイマーが間引かれることがあるので、
  多少のズレは許容し、タブを開き直した時点で再計算する形で構いません）
- 最後に開いていたタブを localStorage に保存し、次回起動時にそのタブから開く

【データ・設定の統合】
- 3本それぞれの localStorage キーが衝突していないか確認してください
  （特に総資産記録帳は、旧・収支ログ実装からのキー変更が正しく反映されているか要確認）
- 「設定」を独立タブとして追加してもよいか、まず提案してください
  （通知の一括ON/OFF、Universalisのワールド設定、手数料率などをまとめられると便利そうなため）
- 全体まとめてJSONエクスポート/インポートできる機能を追加してください
  （個別タブごとのエクスポートに加えて、「全部まとめて」も選べるようにする）

【通知の扱い】
- ノード出現通知が、他タブを見ている間も正しく発火するか確認してください
- どのタブからの通知か分かるように、通知にラベルを付けてください

【技術方針】
- ビルド不要な単一ページ構成を維持（index.html + 複数JSファイル、ESモジュールで分割）
- 外部ライブラリは既存実装で使っているもの（Universalis連携、グラフ描画ライブラリなど）
  以外は極力増やさないこと
- ダークテーマ・日本語UIは既存方針を踏襲

【受け入れ条件】
- タブを切り替えても、他タブの状態（入力中のフォーム含む）が消えない
- ページをリロードしても、最後のタブ・データがすべて復元される
- 3機能ぶんのlocalStorageキーが衝突しない
- ノード通知が正しく発火する
- 将来的に釣りツールなど新しいタブを追加しやすい構造になっている
  （タブ追加の手順を軽くコメントかREADMEに残してもらえると助かります）
```

---

## md ファイルの使い方

1. **置き場所**
   3本のツールをまとめてあるフォルダの直下に、このファイルをそのまま保存してください。
   ファイル名は何でも構いませんが、`INTEGRATION_PROMPT.md` のように分かりやすい名前にしておくと後で見返しやすいです。

2. **念のためバックアップ**
   統合作業は既存の3本のコードを直接触る作業なので、着手前にフォルダをコピーするか、
   gitを使っているなら一旦コミットしておくと、うまくいかなかった時に戻せて安心です。

3. **起動と指示**
   ターミナルでそのフォルダに `cd` してから `claude` を起動し、次のように伝えます。

   ```
   INTEGRATION_PROMPT.md の内容を実行してください。
   このフォルダにある3つの実装済みツールを対象にしてください。
   ```

   ファイル名を直接指定して読ませる形でも構いません。

   ```
   @INTEGRATION_PROMPT.md の指示に従って統合してください。
   ```

4. **最初のやり取り**
   指示文の中に「まずファイル構成を確認して報告してください」という一文を入れてあるので、
   Claude Codeが最初にフォルダの中身を把握した内容を返してくるはずです。
   ここで3本の対応関係が合っているか確認してから、本格的な統合作業に進んでもらう流れになります。

5. **統合後の確認**
   受け入れ条件にある通り、タブ切り替え・リロード後の状態復元・通知の発火あたりは
   実際に触って確認しておくと安心です。特にlocalStorageのキー衝突は、
   総資産記録帳が旧実装のキーを引きずっていないかも合わせて見ておくと良さそうです。
